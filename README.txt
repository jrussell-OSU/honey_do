To run:

LINUX:
1) clone the repo
2) start virtual environment
3) pip install -r requirements
4) run game command:
    python3 -m game


move with arrow keys or WASD
Collect enough honey to save your hive
